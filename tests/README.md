# tests

Automated tests for reusable code live here.

Guidelines:
- test shared code from `src/`
- name files `test_<feature>.py`
- keep quick smoke tests for critical utilities

Experiment-only one-off checks should stay in the experiment folder unless they become reusable.
