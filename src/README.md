# src

Shared code lives here.

Use this directory for reusable modules such as:
- dataset loading
- feature extraction
- model definitions
- training utilities
- inference helpers

Keep experiment-specific scripts inside `experiments/<exp_name>/`.
